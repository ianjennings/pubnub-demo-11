version: 4.0.65
steps:
  - prompt: Click on "Solutions" in the header.
    commands:
      - command: hover-text
        text: Solutions
        description: link in the header
        action: click
  - prompt: Assert that the solutions page loads.
    commands:
      - command: assert
        expect: BY USE CASE is visible on the page
  - prompt: Assert that the solutions page loads.
    commands:
      - command: hover-text
        text: Chat
        description: link in the solutions dropdown
        action: click
      - command: wait-for-text
        text: Chat Solutions
      - command: assert
        expect: Chat Solutions page is visible
  - prompt: >-
      Verify that solution details are visible.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: Build communities with live chat functionality is visible
